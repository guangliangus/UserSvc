{
  "ConnectionStrings": {
    "Default": "Host=localhost;Port=5432;Database=usersvc;Username=usersvc;Password=usersvc"
  },
  "AuthSession": {
    "AccessTokenLifetime": "00:10:00",
    "RefreshTokenLifetime": "30.00:00:00",
    "MaxActiveDevices": 10
  },
  "IdentifierProtection": {
    "Pepper": "",
    "DataKey": "",
    "KeyVersion": "v1"
  },
  "Serilog": {
    "_comment": "Levels only. The sinks come from the template's observability block: compact JSON to stdout (with @tr, the same 32-hex trace id the error contract publishes as traceId) unless Observability:ConsoleFormat is 'text', which appsettings.Development.json sets and which renders [{TraceId}] in the line. A WriteTo here would ADD a second console sink beside the block's, not replace it.",
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning"
      }
    }
  },
  "Observability": {
    "_comment": "Template block. OtlpEndpoint absent means no trace exporter (OTEL_EXPORTER_OTLP_ENDPOINT also works); TraceSampleRatio is the head-sampling ratio for new roots, parent-based, and production lowers it. Metrics are scraped from /metrics, never pushed.",
    "ConsoleFormat": "json",
    "TraceSampleRatio": 1.0
  },
  "Postgres": {
    "_comment": "Template block. The EF retry budget and the per-statement timeout; before it the retry was a bare default and there was no command timeout at all.",
    "MaxRetryCount": 5,
    "MaxRetryDelaySeconds": 30,
    "CommandTimeoutSeconds": 30
  },
  "ForwardedHeaders": {
    "_comment": "Template block, OFF here on purpose - a developer box is called directly. The deployment behind the gateway sets Enabled=true and KnownNetworks to the ingress CIDR (empty KnownNetworks trusts every source, like ASPNETCORE_FORWARDEDHEADERS_ENABLED). Until it is on, every per-source budget in RedisRateLimiter sees the gateway's address: docs/architecture.md, the rate-limiting section.",
    "Enabled": false,
    "KnownNetworks": [],
    "KnownProxies": [],
    "ForwardLimit": 1
  },
  "RequestTimeouts": {
    "_comment": "Template block. The inbound budget; on expiry a 504 ProblemDetails carrying this errorCode. Named policies for [RequestTimeout(\"name\")] endpoints go under Policies as seconds.",
    "Enabled": true,
    "DefaultTimeoutSeconds": 30,
    "ErrorCode": "REQUEST_TIMEOUT",
    "Policies": {}
  },
  "Cors": {
    "_comment": "Template block, inactive while Origins is empty. The gateway terminates CORS today.",
    "Origins": []
  },
  "Tasks": {
    "_comment": "The generic async task queue over identity.task_queues (db/0014_task_queues.sql). WorkerCount=0 is the kill switch AND the shipped default: both hosted services read it at startup and return - nothing is polled, no timer runs, no connection is taken - and each says so in one log line, because \"is this pod working the queue\" has no endpoint to ask. It is also the switch that makes one image serve two deployment shapes (decision 02): API pods stay at 0, worker pods set a positive number. The other knobs - MaxAttempts, ShortPollInterval, LongPollInterval, StalePoppedTimeout, ReclaimInterval, TaskTimeout, DrainTimeout - all have working defaults on TaskQueueOptions and are deliberately not repeated here, so there is only one place for them to drift. Note that this service has no handler yet (the Go side's only one is the FCM topic sync, which belongs to the notification service), so raising WorkerCount today buys one log line saying no handler is registered.",
    "WorkerCount": 0
  },
  "AllowedHosts": "*",
  "_comment": "Redis:Configuration and Notification:BaseAddress are deliberately absent. Both are [Required] and validated at startup, so a deployment without them refuses to boot instead of silently pointing at the wrong host. They arrive from Key Vault / ExternalSecrets as Redis__Configuration and Notification__BaseAddress. BackOfficeSignIn__SignInTicketKey belongs to the same set and is absent for the same reason, but is NOT validated at startup: a deployment without it boots and serves everything except the two back-office sign-in endpoints, which answer 500 NOT_CONFIGURED naming the key. Every replica must hold the SAME value - the ticket is minted by one pod and redeemed by another. StaffDirectory__* is optional in the same way and gates only the corporate one-time-password door."
}
